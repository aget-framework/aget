# Release Notes: v3.20.0

**Released**: 2026-05-30
**Theme**: Debt Paydown + Structural-Guard Deployment + Functional Capability
**Upgrade**: version-bump only — no breaking changes

## Overview

v3.20.0 spends an accumulated reliability dividend on three fronts:

1. **Debt paydown** — resolved the long-standing release-citation 404s on published surfaces (R-REL-044 / CAP-REL-035), and fixed a duplicate-`CAP-REL-035` declaration that had scored the entire 224-requirement `AGET_RELEASE_SPEC` as NONE (now L5).
2. **Structural-guard deployment** — a close-gate conformance check (blocks marking a PROJECT_PLAN COMPLETE with unchecked V-tests), health/wind-down signal-class severity, and the "verify with the consumer's own check" rule, all wired so release-and-close discipline is enforced rather than advisory.
3. **Functional capability** — `/aget-propose-actions`, the highest-frequency session surface, now presents an evidence column, a lead recommendation, and an explicit decisions-needed callout.

See `CHANGELOG.md` `## [3.20.0]` for the itemized list and `handoffs/RELEASE_HANDOFF_v3.20.0.md` for the fleet upgrade guide.

## Decisions

### D-1: DEPLOYMENT_SPEC — Option B (inherited)

No `DEPLOYMENT_SPEC_v3.20.0.yaml` is shipped. v3.20.0 inherits the `DEPLOYMENT_SPEC_v3.16.0.yaml` contract semantically — there is **no deployment-contract change** in this release. This is consistent with v3.17, v3.18, and v3.19, which also inherited v3.16.0. Recorded as the SOP Phase 3.0(b) Option-B explicit-deferral disclosure (the pre-tag inventory correctly reports the file ABSENT; this Decision is the sanctioned override). Documented also in the handoff §DEPLOYMENT_SPEC Note.

### D-2: Delta spec — not produced (additive release)

No `specs/deltas/AGET_DELTA_v3.20.md` is shipped. v3.20.0's changes are additive (new skill presentation, structural guards, fixes) with no spec-contract delta requiring a formal delta document. Consistent with v3.18/v3.19 (no delta specs).

## Notes

The cycle's internal anti-confabulation and audit-after-synthesis disciplines (which governed how the release itself was built and verified) are documented in the framework's internal evolution record.
