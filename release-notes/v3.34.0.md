# AGET v3.34.0 — Evidence that reaches its subject

Release candidate prepared 2026-09-13. This document does not assert publication or downstream acceptance.

## What changes

This release improves receipt continuity, template integrity, canonical discovery, and the evidence used to judge runtime, host, voice and release behavior. It carries the following fifteen selected outcomes.

| Outcome | Change | Shipped entry point |
|---|---|---|
| C-34-01: Routable template skills | Strict YAML descriptions and scaffold validation reject absent or invalid metadata. | `verification/validate_archetype_skills.py` |
| C-34-02: Receipt continuity | Unrelated history and dirt do not expire an accepted receipt; target-touching history, including touch-and-revert, rejects continuity. | `scripts/check_receipt_continuity.py` |
| C-34-03: Lint ratchet and capability retention | Producer lint ratchet rejects regression against the shipped baseline; capability retention rejects destructive removal. The inherited lint backlog is not cleaned. | `scripts/check_payload_lint.py` |
| C-34-04: Canonical discovery | Study-topic resolves explicit and portfolio-parent canonical sources and discloses the searched scope on a miss. | `scripts/study_topic.py` |
| C-34-05: Template integrity | Core and all 13 registered templates carry matching release payload and version metadata. Publication must preserve exact reviewed commits. | `scripts/health_check.py` |
| C-34-06: Copyable worker template | Removes the provenance-confirmed vestigial worker housekeeping symlink; packaging must retain no dangling script links. | `scripts/instantiate_template.py` |
| C-34-08: Receiver conformance | Resolves explicit canonical sources, checks declared payload digests, and retains INCOMPLETE when actual receipt acceptance is pending. | `scripts/check_receiver_conformance.py` |
| C-34-17: Value-gate forecast | Non-mutating forecast reports all declared limbs, including unavailable evidence and non-firing conditions. | `scripts/forecast_value_gate.py` |
| C-34-18: Runtime evidence | Separates executable discovery, instruction discovery and exercised runtime behavior; a desktop launcher does not establish terminal-agent support. | `scripts/check_runtime_support_evidence.py` |
| C-34-23: Ontology grounding | Case-aware matching binds canonical concepts while preserving boundaries, aliases and honest no-match reporting. | `scripts/ground_artifact.py` |
| C-34-24: Host-layout conformance | Checks declared manifest, roster and readable host envelope; does not claim remote deployment from fixture results. | `scripts/check_host_layout_conformance.py` |
| C-34-25: Voice consumption | Binds the briefing skill to governed voice inputs and checks consumer references; downstream receiver adoption still requires receiver-owned evidence. | `scripts/check_voice_consumer_binding.py` |
| C-34-26: Principal-impact triage | Freezes and verifies an explicitly named repository cohort, including digest and label reconciliation. | `scripts/freeze_triage_cohort.py` |
| C-34-27: Standards bindings | Checks declared receipt vocabulary mappings and rejects equivalence claims unsupported by scope or cryptographic properties. | `scripts/check_standard_bindings.py` |
| C-34-30: Health-check accuracy | Normalizes lesson IDs, reports the measured governance population, and removes the inert repair flag and stale guidance. | `scripts/health_check.py` |

## Known limitations and regression coverage

Citation resolution carries an explicit, principal-authorized exception for this release. The retained correctly rooted check examined 64 artifacts and 773 citation occurrences: 88 resolved, 200 annotated exemptions, and 485 unannotated unresolved occurrences. Its result remains FAIL; this is not a claim that citations pass or that every occurrence was individually adjudicated. The framework manager owns next-cycle resolution and verification. A zero-input scan is invalid evidence and does not support readiness.
Two further principal-authorized, release-specific carries apply to private manager assurance. Qualified independent review bound 104 of 127 examined test methods; 23 remain without qualified governing-source bindings. Three existing assurance tests also remain failed: the reference consumer measures 1,329 of 2,850 methods across 277 top-level test files (46.63% against a 69% floor), and two file-level checks identify 20 files without the required references. These are the measured scopes of those consumers, not all recursive tests. The failures remain FAIL; neither existing CI treatment nor these carries establishes correct behavior, expands an exclusion list, or changes a debt baseline. The framework manager owns next-cycle resolution and verification. Changed populations or results require explicit applicability review.


The accepted successor payload is commit `1a87e0743c42727aab36128911fc8a4c89692398`, tree `0a0058a4366dd5428ac6b8e2b2a69c49d355bed5`. Its sole parent, original seal `72633e6d1b58e94541a59892d30c0b4bbd9d20f1`, remains immutable. One successor incorporates the independently reviewed 21-path repair delta. Release packaging adds metadata and handoff artifacts.

Original acceptance recorded a regression-coverage shortfall: RM-3, RM-4, RM-8 and RM-11 survived the original shipped source-substring tests, while independent non-shipping cases rejected all four. That review exercised 27 discriminating checks plus one observation. Successor repairs replace two brittle source assertions with behavioral checks and add rejecting cases for discovered defects. The four historical mutation classes have not all been re-scored against the successor; their disclosure and next-cycle verification obligation remain carried, rather than being declared closed by the new test count.

The lint ratchet passes at 436 findings against the unchanged 437 baseline with Ruff 0.15.12; inherited debt remains. Formatting-only transformations were proven AST-identical before separately reviewed import and behavioral changes. The source-bound successor suite passed 973 tests with 11 disclosed skips and 13 passing subtests. All nine new instruments were run: missing host/runtime/receiver evidence, the raw-template parsing limit and static-audit classification limits remain explicit. A missing tool, subject or required input is not a pass.

Original independent review used a separate actor with producer-hosted permission handling; its permission, coaching and custody limitations remain recorded. Renewed full-delta acceptance used a separate reviewer context on the same harness/model family and machine. That reviewer made no candidate edits and independently verified the successor commit/tree binding. Candidate acceptance is distinct from final release-quality acceptance, publication and receiver delivery.

## Final packaging corrections

Template study-topic instructions now invoke the shipped `scripts/study_topic.py` implementation. The document-processor template includes its advertised project, proposal and skill scaffolds; the same source scaffolds are available in core. Checks exercised the study command at all registered repository roots and verified the scaffold bytes. Scaffold availability does not establish conformance of every generated artifact or resolve every reference it contains.

The friction recorder's documented hook command now uses the project root and an explicit nonblocking fallback. Verification exercised launch from a subdirectory and an absent script target. Existing receiver hook settings are not modified by this documentation correction. These packaging changes require final review of the updated publication commits; historical candidate acceptance is not acceptance of the changed packaging.

## Additional release-specific carries

Two further inherited control failures have explicit principal-authorized release exceptions. The skill validator reports a required baseline of 29 and a worker-derived baseline of 35, an alignment failure of six; its successful presence-check exit does not clear that failure. The worker contains 38 skills, with three release-execution extras excluded from its derived baseline. The six additional names are `aget-ask`, `aget-close-project`, `aget-create-goal`, `aget-create-initiative`, `aget-propose-goals` and `aget-propose-initiative`. All 29 mandated names are present in core and all thirteen templates. For this release only, the principal explicitly overrides the requirement to halt on this reported drift. Neither 35 as a new normative mandate nor correct validator exit behavior is asserted. The framework manager owns baseline reconciliation and the alignment-versus-exit defect at the first post-release maintenance intake, before next-version scope.

The retained pre-cut verb audit also reports one gating unregistered skill verb, `ground`. Its required hierarchy disposition has not been established in the reviewed vocabulary. The principal explicitly permits this release despite that unresolved pre-cut requirement; the raw failure remains failed, and the vocabulary is not represented as repaired. This exception covers only that one skill verb, not the separate 59 nongating script tokens. The framework manager owns formal disposition and verification at the same post-release intake. These exceptions do not extend any other carry or authorize further remediation within this release window.

A release-specific principal disposition carries an unresolved historical authorization evidence gap: prior-release closure is recorded and later corroborated, but the original authorization event has not been established from the cited primary source. That evidence remains unavailable; this release does not claim that it was verified or that the prior closure was unauthorized. The framework manager owns source-provenance follow-up at the first post-release maintenance intake. This disposition covers this evidence gap only.

After the documented consumer repairs, the unchanged dependency checker still reports 1,035 missing repository–skill–path entries across the thirteen registered templates, down from 1,052 with 17 resolved and none added. The principal authorized carrying this exact classified population for this release. It includes operational inputs, conditional and optional references, examples, generated-output locations, prohibited edit targets, extraction fragments, and references whose applicability is unresolved. These are not 1,035 demonstrated runtime defects, and they are not all established false positives. The raw checks remain FAIL. Installed receiver behavior and instantiation-supplied inputs are not established by template-tree absence checks.

The framework manager owns per-group disposition and rerun evidence at the first post-release maintenance intake, before deciding next-version scope. No additional failure population is covered. Incoming follow-ups are also explicitly carried: legacy session-note writer disposition; remaining public propagation of the repaired hook documentation; the four reported producer issues covering historical payload lint, missing configuration checking, deployment-spec state and migration provenance; and the named-instance and class follow-up for platform-specific CI paths. The earlier lint-only report is consolidated into the four-issue report without closing its underlying lint obligation. These dispositions permit release progression; they do not establish that the reported behavior is correct or that delivery has occurred.

## Upgrade and compatibility

Read [the release handoff](../handoffs/RELEASE_HANDOFF_v3.34.0.md) and [deployment specification](../DEPLOYMENT_SPEC_v3.34.0.yaml) from the peeled public tag before installation. Preserve receiver-owned extensions and compare local modifications before replacing framework files. Python 3.10 or newer is required.

Publication, installation, behavior verification, and acceptance are separate states. No receiver adoption is asserted here. A receiver-owned receipt must name its actual source pins, credited capability and exercised rejection path; producer-written receipts do not count.
