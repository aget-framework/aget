# v3.27.0 — Finish & Verify

**Released**: 2026-07-18 · **Type**: minor · **Theme**: the portfolio learns to FINISH what it starts, and the release learns to VERIFY that what it shipped actually runs.

## Why this release

Two chronic gaps drove the theme. First: a 0-COMPLETE portfolio — 38 exit conditions across 11 active initiatives, zero ticked, while at least 4 verified met at source. The EC layer was decorative. Second: v3.26's sweep week proved "31/31 upgraded ≠ 31/31 unregressed" — receipt verification confirms landing, not running.

## Finish

- **CIS-010 EC tick-state** becomes a maintained, surfaced signal (first run: WARN 0/33 — honest).
- **CIS-008 capability share** re-specified to Achieve-only (permanent stewards no longer dilute; the check stops punishing completion). First computation: honest FAIL 2/9, accepted-with-eyes-open.
- **L-doc ID injectivity** restored: 10 collided files renumbered with a tombstone registry, collision FAIL armed.
- NASCENT manifests typed with Health Contracts; the portfolio walked real terminal transitions (2 CLOSED with accounting, 2 FOLDED with streams traveling) — the closes REFUSED a false COMPLETE on verification, and the refusal held.

## Verify

- **Migration Rung 4** (SOP_fleet_migration v1.7.0): behavioral smoke probes on the EXECUTED surface + post-payload test suite + dual-basename parity; pilot confirmation now requires a behavioral result.
- **3-axis tag-payload coherence**: all 14 origins × tagged-tree paths × executed-surface declarations (DEPLOYMENT_SPEC v3.27.0 carries the first binding declarations).
- **Corrections-since-tag single surface**: one write per post-tag fix; guidance surfaces point, never duplicate.
- DoD gains ID-axis row-completeness ratcheting and public-graph coherence; a verify-before-claim coverage matrix makes channel gaps inspectable.

## Also shipped

Transactional Execution doctrine + workspace convention in every template AGENTS.md; universal-skill conformance 14/14 (core now conforms by ruling); dead single-slash permission-rule detector; dual-status mask scanner; wind-down double-fire guard; delegated-prompt date contract; Filing_Candidate L-doc marker.

*Deep notes convention: release-notes/ per R-REL narrative discipline.*
