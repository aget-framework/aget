# v3.29 Claude Code ↔ Codex CLI control-outcome matrix

| Outcome | Claude Code | Codex CLI |
|---|---|---|
| Applicability | Registered `PreToolUse` hooks are the installed substrate; a sanitized portable fixture reproduces acceptance without installation | Isolated 0.144.5+ POC models the supported event contract; adoption remains a separate governed config change |
| Trust / permission boundary | Permission prompts remain operative | Untrusted projects are reported `unavailable`, never enforcing PASS; no `trust_level` change |
| Positive block | Existing registered guards provide blocking routes | Sensitive governance surface without individual affirmation blocks |
| Negative control | Ordinary commands pass registered precision guards | Ordinary paths pass |
| Denied command side effect | Guard denial occurs before the tool call | POC is pure and never executes the proposed operation |

Only Claude Code and Codex CLI are in the v3.29 supported-client set. Other clients are N/A rather than
implicitly PASS. The Codex POC proves the outcome contract without editing `.codex/config.toml`, hooks,
skills, or trust; installation/adoption requires its own operator-visible affirmation. The Claude fixture is
also non-installing and is labeled as fixture evidence rather than live registration.
