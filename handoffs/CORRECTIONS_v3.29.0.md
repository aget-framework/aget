# CORRECTIONS — v3.29.0

**Status**: ACTIVE

This is the single public disclosure surface for defects found after the immutable v3.29.0 tags were
published. Tags are not moved or replaced. The public `main` branch and mutable GitHub Release records
carry the fix-forwards below.

| # | Surface at `v3.29.0` | Correction on `main` / live service | Consumer action |
|---:|---|---|---|
| 1 | All 14 GitHub Release bodies reused the 62-line deep release notes and failed the canonical 3-section, 12–25-line body contract. | Bodies replaced with the validated `release-notes/v3.29.0_release_body.md` source; all 14 live validators PASS. | No tag change. Read the GitHub Release body for the concise overview and the tag-pinned release notes for depth. |
| 2 | `docs/VERSION_HISTORY.md` remained current only through v3.27.0. | Public-core `main` commit `53468ea` adds v3.28.0 and v3.29.0 current/timeline entries. | Use `main` for the current timeline; historical tag payload remains unchanged. |
| 3 | `handoffs/RELEASE_HANDOFF_v3.29.0.md` contained external migration content but omitted the required `Context for External Fleets` heading. | Public-core `main` commit `70d49ef` adds the explicit section and points back to this corrections surface. | External fleets should read the corrected handoff on `main` before migration. |
| 4 | The manager's three-axis tag checker treated every path in every M-row detection clause as required at all 14 tags, ignoring seat applicability and the declared canonical distribution point. | Public-core `main` commit `5ce6487` makes the checker honor structured `tagged_origins` and check `distribution_point` paths for scoped rows; v3.29 declares canonical plus advisor-exemplar coverage per the locked release plan. | No payload copy is implied for non-applicable template tags; receiving seats still copy the declared canonical distribution point before running applicable detections. |
| 5 | `DELIVERED_FILES_v3.29.0.yaml` omitted `docs/POSITION_agents_instruction_reach_and_self_amendment.md`, while the tagged POSITION prose satisfied only 1/4 marker patterns enforced by M-3.29-1. Adding that tagged document verbatim would leave 16/32 seats failing in the reporting supervisor population. | Public-core `main` commit `52681fb` aligns the POSITION vocabulary with all four canonical marker predicates, adds the file and exact hash to the delivered-files manifest, and adds it to M-3.29-1's distribution point. The immutable tag is unchanged. | Pin the tag for original payload, then read correction #5 and copy the POSITION document using the manifest on `main`. Run M-3.29-1 at the receiving seat. Regex acceptance is not received-state or ordinary-use evidence. |
| 6 | `tests/test_study_topic_purpose_recency_rendering.py` prefers a co-located sibling `../aget/scripts/study_topic.py` over the receiving seat's own script. In the framework-manager checkout this produced a live false-green: default 4/4 PASS, while forcing the local divergent script failed 4/4. | Public-core `main` commit `bf5f96a` defaults to the receiving checkout's own `scripts/study_topic.py`, preserves `AGET_STUDY_TOPIC_SCRIPT` only as an explicit test override, adds selector-contract tests, and updates the delivered-files manifest to the exact corrected test hash. The immutable tag is unchanged. | Read correction #6 and use the corrected test from `main` before claiming M-3.29-6 at a receiver. Run it from the receiving checkout; an explicit override is for controlled falsifiers only. Test execution is not ordinary-use wiring evidence. |
| 7 | `scripts/check_cross_client_hook_controls.py` treats Claude applicability as `PreToolUse` hooks present **and** the substring `guard` appearing somewhere in their serialized command text. A registered command hook with a different filename therefore fails, while malformed prose containing `guard` can satisfy the proxy. | Public-core `main` commit `d7b5f91` measures registered `PreToolUse` command-hook structure instead of filename prose, adds both-polarity regressions, updates the exact manifest hash, and makes the handoff's post-tag path correction-only and schema-semantic. The immutable tag is unchanged. | Read correction #7 and use the corrected validator + manifest from `main` before claiming M-3.29-2. For an already-v3.29 seat, copy only changed correction paths; do not replay migration or append history. |
| 8 | The public handoff pilot table still marked producer migration and downstream receipt `pending` after release Gate 2 and independent downstream received-state had passed. | Public-core `main` commit `93ad5af` records the producer at v3.29 and one downstream receiver verified at source, while retaining the cold-context behavior leg as pending and withholding the private receiver identity. | Read received-state and ordinary-use behavior as separate columns; a verified version does not satisfy the pending cold-context behavior leg. |

These corrections do not establish downstream receipt or behavior. Gate 4 remains separately authorized
and must prove the receiving version plus cold-context Codex discovery, invocation, and recovery.
